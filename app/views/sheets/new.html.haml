%article.sheet.new

  %ul.breadcrumb
    %li
      = link_to t('nav.sheets.index'), sheets_path
      %span.divider /
    %li.active
      = t('nav.sheets.new')

  %header.page-header
    %h1.pull-left
      = t('.title')

  = render :partial => 'form'

