<%= content_tag :li, :class => 'next' + (current_page.last? ? ' disabled' : '') do %>
  <% if current_page.last? %>
    <%= link_to raw(t 'views.pagination.next'), '', :rel => 'next', :remote => remote %>
  <% else %>
    <%= link_to raw(t 'views.pagination.next'), url, :rel => 'next', :remote => remote %>
  <% end %>
<% end %>
