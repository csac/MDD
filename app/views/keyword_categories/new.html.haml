%article.keyword-category

  %ul.breadcrumb.row
    %li
      = link_to t('nav.keyword_categories.index'), keywords_path
      %span.divider /
    %li.active
      = t('nav.keyword_categories.new')

  %header.page-header.row
    %h1.pull-left
      = t('.title')

  = render :partial => 'form'

